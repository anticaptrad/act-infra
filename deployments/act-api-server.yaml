# act-api-server — HTTP API + NATS event-bus bridge (Rust/axum).
# Deployed to the k8s cluster at ~/codes/ores/k8s-cluster.
#
# Secrets are provided by the `act-api-server-secrets` Secret, which the fiducia
# external-secrets operator populates from fiducia.cloud (no in-cluster mock
# loader, no secrets written to disk). Non-secret config is in a ConfigMap.
apiVersion: v1
kind: ConfigMap
metadata:
  name: act-api-server-config
  labels:
    app: act-api-server
data:
  PORT: "8080"
  OTEL_SERVICE_NAME: "act-api-server"
  OTEL_EXPORTER_OTLP_ENDPOINT: "http://otel-collector:4317"
  NATS_URL: "nats://nats:4222"
  RUST_LOG: "info,act_api_server=debug"
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: act-api-server
  labels:
    app: act-api-server
spec:
  replicas: 2
  selector:
    matchLabels:
      app: act-api-server
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 0
      maxSurge: 1
  template:
    metadata:
      labels:
        app: act-api-server
    spec:
      securityContext:
        runAsNonRoot: true
        runAsUser: 10001
        fsGroup: 10001
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: app
          image: anticaptrad/act-api-server:latest
          imagePullPolicy: IfNotPresent
          ports:
            - name: http
              containerPort: 8080
          envFrom:
            - configMapRef:
                name: act-api-server-config
            - secretRef:
                name: act-api-server-secrets
                optional: true
          resources:
            requests:
              cpu: 50m
              memory: 64Mi
            limits:
              cpu: 500m
              memory: 256Mi
          livenessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 5
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /ready
              port: http
            initialDelaySeconds: 3
            periodSeconds: 10
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop:
                - ALL
---
apiVersion: v1
kind: Service
metadata:
  name: act-api-server
  labels:
    app: act-api-server
spec:
  selector:
    app: act-api-server
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: http
